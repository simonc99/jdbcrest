http {

    limit_req_zone \
        $binary_remote_addr \
        zone=api_limit:10m \
        rate=10r/s;

}

location / {

    limit_req zone=api_limit burst=20;

    proxy_pass http://jdbc_api;
}
